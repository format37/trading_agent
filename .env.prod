# ============================================================================
# Trading Agent Production Environment Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# API Keys (REQUIRED)
# ----------------------------------------------------------------------------
# Anthropic API Key - Get from https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ----------------------------------------------------------------------------
# Service Configuration
# ----------------------------------------------------------------------------
# Service port (internal)
PORT=8012

# Host binding
HOST=0.0.0.0

# Environment
ENVIRONMENT=production

# Log level (debug, info, warning, error)
LOG_LEVEL=info

# ----------------------------------------------------------------------------
# Authentication (Token-based middleware)
# ----------------------------------------------------------------------------
# Enable authentication (true/false)
AGENT_REQUIRE_AUTH=true

# Allowed tokens (comma-separated)
# Generate strong tokens with: openssl rand -hex 32
AGENT_TOKENS=your_secure_token_here,another_token_if_needed

# ----------------------------------------------------------------------------
# Telemetry Configuration
# ----------------------------------------------------------------------------
# Telemetry is DISABLED by default for production simplicity
ENABLE_TELEMETRY=false

# If you want to enable telemetry, uncomment and configure:
# ENABLE_TELEMETRY=true
# OTEL_EXPORTER_TYPE=otlp-grpc
# OTEL_EXPORTER_OTLP_ENDPOINT=http://signoz-otel-collector:4317
# OTEL_SERVICE_NAME=trading-agent

# ----------------------------------------------------------------------------
# MCP Server URLs (Docker Network)
# ----------------------------------------------------------------------------
# These URLs use Docker service names on the mcp-shared network
# Polygon MCP - Market data
POLYGON_URL=http://mcp-polygon:8009/polygon/

# Binance MCP - Trading execution
BINANCE_URL=http://mcp-binance:8010/binance/

# Perplexity MCP - Market intelligence
PERPLEXITY_URL=http://mcp-perplexity-local:8011/perplexity/

# ----------------------------------------------------------------------------
# Data Storage
# ----------------------------------------------------------------------------
# Data directory for CSV storage (mounted volume)
DATA_DIR=/app/data

# Trading agent specific data directory
AGENT_DATA_DIR=/app/data/trading_agent

# ----------------------------------------------------------------------------
# Optional: Sentry Error Tracking
# ----------------------------------------------------------------------------
# SENTRY_DSN=your_sentry_dsn_here

# ----------------------------------------------------------------------------
# IMPORTANT NOTES
# ----------------------------------------------------------------------------
# 1. Replace all placeholder values with actual production values
# 2. Keep this file secure - it contains sensitive credentials
# 3. Never commit this file to version control
# 4. Generate strong random tokens for AGENT_TOKENS
# 5. MCP servers must be running and accessible on the mcp-shared network
